equipments = {
	
	### Helpful handy information: modules that depend on other modules (ie, big guns require fixed turrets)
	### require that the module slots for their equipment archetype are defined in operational order for AI + historical templates.
	### tldr; If your gun requires a specific turret: define the turret slot first.
	
	mbt_chassis = {
		year = 1991
		is_archetype = yes
		is_buildable = no
		picture = archetype_mbt_tank_equipment
		type = armor
		group_by = archetype
		interface_category = interface_category_armor
		priority = 2000

		module_slots = {

			turret_type_slot = {
				required = yes
				allowed_module_categories = {
					tank_turret_type
				}
			}
			
			main_armament_slot = {
				required = yes
				allowed_module_categories = {
					tank_barrel_rifled
					tank_barrel_smoothbore
				}
			}

			suspension_type_slot = {
				required = yes
				allowed_module_categories = {
					tank_suspension_type
				}
			}

			armor_type_slot = {
				required = yes
				allowed_module_categories = {
					tank_steel_armor
					tank_comp_armor
				}
			}

			engine_type_slot = {
				required = yes
				allowed_module_categories = {
					tank_diesel_engine
					tank_turbine_engine
					tank_electric_engine
				}
			}

			special_type_slot_1 = {
				required = no
				allowed_module_categories = {
					tank_special_module
					tank_radio_module
				}
			}

			special_type_slot_2 = {
				required = no
				allowed_module_categories = {
					tank_special_module
					tank_radio_module
				}
			}

			special_type_slot_3 = {
				required = no
				allowed_module_categories = {
					tank_special_module
				}
			}

			special_type_slot_4 = {
				required = no
				allowed_module_categories = {
					tank_special_module
				}
			}

		}
		
		module_count_limit = {
			category = tank_radio_module
			count < 2
		}

		module_count_limit = {
			module = auto_loading
			count < 1
		}

		module_count_limit = {
			module = mangal_cope_cage
			count < 1
		}

		default_modules = {
			main_armament_slot = empty
			turret_type_slot = tank_three_man_turret
			suspension_type_slot = tank_bogie_suspension
			armor_type_slot = tank_steel_armor_gen1
			engine_type_slot = tank_diesel_engine_gen2
		}

		fuel_consumption = 0
		maximum_speed = 7
		build_cost_ic = 6
		reliability = 1.2
		armor_value = 65
		hardness = 0.95
		resources = {
			steel = 2
			chromium = 1
		}

		lend_lease_cost = 13

		manpower = 2

	}
}

