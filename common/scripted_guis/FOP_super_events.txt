scripted_gui = {

	sp_event_window = {
		window_name = "sp_event_window"
		context_type = player_context

		visible = {
			has_global_flag = Super_Event_Visible
		}
		
		triggers = {
			Super_Event_odessa_war = {
				has_global_flag = super_odessa_war
			}
		}
		
		############
		###BUTTON###
		############
		
		effects = {
			sp_event_close_button_click = {
				clr_global_flag = Super_Event_Visible
				
				if = {
					limit = {
						has_global_flag = super_odessa_war
					}
					clr_global_flag = super_odessa_war
				}
			}
		}
	}
}

scripted_gui = {

	sp_event_window = {
		window_name = "sp_event_window"
		context_type = player_context

		visible = {
			has_global_flag = Super_Event_Visible
		}
		
		triggers = {
					Super_Event_russian_ukrainian_war = {
				has_global_flag = super_russian_ukrainian_war
			}
		}
		
		############
		###BUTTON###
		############
		
		effects = {
			sp_event_close_button_click = {
				clr_global_flag = Super_Event_Visible
				
				if = {
					limit = {
						has_global_flag = super_russian_ukrainian_war
					}
					clr_global_flag = super_russian_ukrainian_war
				}
			}
		}
	}
}