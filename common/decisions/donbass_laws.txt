donbass_laws = {
	medicine = {
		available = {
		has_idea = healthcare_public_bad
	}
		visible = {
						OR = {
			tag = DPR
			tag = LPR
		}
		}
		days_re_enable = 2

		cost = 100
		days_remove = 180

		complete_effect = {
 		add_timed_idea = {
    idea = healthcare_public_decent
    days = 180
}
}
		}
		
	invest_in_army = {
		available = {
		NOT = {
		has_idea = finances_civilian
		}
	}
		visible = {
						OR = {
			tag = DPR
			tag = LPR
		}
		}
		days_re_enable = 2

		cost = 80
		days_remove = 180

		complete_effect = {
 		add_timed_idea = {
    idea = finances_army
    days = 180
} 
}
		}
	invest_in_civilian = {
		available = {
		NOT = {
		has_idea = finances_army
		}
	}
		visible = {
						OR = {
			tag = DPR
			tag = LPR
		}
		}
		days_re_enable = 2

		cost = 80
		days_remove = 180

		complete_effect = {
		add_timed_idea = {
    idea = finances_civilian
    days = 180
}
}
		}
	tighten_customs_control = {
		available = {
custom_trigger_tooltip = {
    tooltip = tamohnya_not_deal	
DPR = {	
		NOT = {
has_completed_focus = dnr_end_the_trade_war
}
    }
}
}
		visible = {
						OR = {
			tag = DPR
			tag = LPR
		}
		}
		days_re_enable = 2

custom_cost_trigger = {
            command_power > 14
        }
        complete_effect = {
            hidden_effect = {
                add_command_power = -15
            }
        }
		custom_cost_text = decision_cost_CP_15
		days_remove = 2

remove_effect = {
add_political_power = 200	
}
		}
		
	hiveration_of_daunbas_cum_to_power = {
		available = {
tag = DPR
}
		visible = {
DPR = {
has_country_flag = DNR_no
has_political_power > 200
command_power > 1
has_idea = finances_civilian
has_idea = education_propagandized
has_idea = healthcare_public_bad
}
		}
		
		cost = 10
		
		fire_only_once = yes

        complete_effect = {
		custom_effect_tooltip = decision_HIVeration_of_donbas_oh_no
            hidden_effect = {
                add_command_power = -1
add_country_leader_role = {
    character = DNR_vasiliev
    promote_leader = yes
    country_leader = {
        ideology = queer_nationalism
        expire = "2222.1.1.1"
        traits = {  }
    }
}
set_politics = {
	ruling_party = radical_socialism
    elections_allowed = no
    long_name = "Novie Lyudi"
    name = "New Humans"
}
set_cosmetic_tag = DPR_HIV
            }
        }
		}

}