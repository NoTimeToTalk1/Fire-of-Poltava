danilov_hunting = {

    kill_pushilin = {			

		icon = GFX_decision_eng_trade_unions_support
		
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}		

		visible = {	
		tag = AFU
		}		

		cost = 50
		fire_only_once = yes

		remove_effect = {
		set_country_flag = try_to_kill_pushilin
	   }
		ai_will_do = {	
			factor = 100		
		}
   }
   
	kill_pushilin_1 = {
	
	icon = GFX_decision_generic_ignite_civil_war
	
			ai_will_do = {	
			factor = 20		
		}
	
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}
		visible = {
        has_country_flag = try_to_kill_pushilin
		}

custom_cost_trigger = {
            command_power > 24
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -25
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_pushilin
            }
        }
		custom_cost_text = decision_cost_CP_25
remove_effect = {
random_list = {
 10 = {
 country_event = { id = dan.31 days = 2 } }
 90 = {
 country_event = { id = dan.32 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }
}
		}
		
	kill_pushilin_2 = {
	
			ai_will_do = {	
			factor = 25		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_pushilin
		}

custom_cost_trigger = {
            command_power > 49
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -50
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_pushilin
            }
        }
		custom_cost_text = decision_cost_CP_50
remove_effect = {
random_list = {
 30 = {
 country_event = { id = dan.31 days = 2 } }
 70 = {
 country_event = { id = dan.32 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
	kill_pushilin_3 = {
	
			ai_will_do = {	
			factor = 50		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_pushilin
		}

custom_cost_trigger = {
            command_power > 74
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -75
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_pushilin
            }
        }
		custom_cost_text = decision_cost_CP_75
remove_effect = {
random_list = {
 50 = {
 country_event = { id = dan.31 days = 2 } }
 50 = {
 country_event = { id = dan.32 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
		
    kill_pasechnik = {	
	

		icon = GFX_decision_eng_trade_unions_support
		
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}		

		visible = {	
		tag = AFU
		}		

		cost = 50
		fire_only_once = yes

		remove_effect = {
		set_country_flag = try_to_kill_pasechnik
	   }
		ai_will_do = {	
			factor = 100		
		}
   }
   
	kill_pasechnik_1 = {
	
			ai_will_do = {	
			factor = 20		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}
		visible = {
        has_country_flag = try_to_kill_pasechnik
		}

custom_cost_trigger = {
            command_power > 24
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -25
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_pasechnik
            }
        }
		custom_cost_text = decision_cost_CP_25
remove_effect = {
random_list = {
 10 = {
 country_event = { id = dan.33 days = 2 } }
 90 = {
 country_event = { id = dan.34 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
		
	kill_pasechnik_2 = {
	
			ai_will_do = {	
			factor = 25		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_pasechnik
		}

custom_cost_trigger = {
            command_power > 49
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -50
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_pasechnik
            }
        }
		custom_cost_text = decision_cost_CP_50
remove_effect = {
random_list = {
 30 = {
 country_event = { id = dan.33 days = 2 } }
 70 = {
 country_event = { id = dan.34 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
	kill_pasechnik_3 = {
	
			ai_will_do = {	
			factor = 50		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_pasechnik
		}

custom_cost_trigger = {
            command_power > 74
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -75
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_pasechnik
            }
        }
		custom_cost_text = decision_cost_CP_75
remove_effect = {
random_list = {
 50 = {
 country_event = { id = dan.33 days = 2 } }
 50 = {
 country_event = { id = dan.34 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
		
    kill_gubarev = {			

		icon = GFX_decision_eng_trade_unions_support
		
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}		

		visible = {	
		tag = AFU
		}		

		cost = 50
		fire_only_once = yes

		remove_effect = {
		set_country_flag = try_to_kill_gubarev
	   }
		ai_will_do = {	
			factor = 100		
		}
   }
   
	kill_gubarev_1 = {
	
			ai_will_do = {	
			factor = 20		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}
		visible = {
        has_country_flag = try_to_kill_gubarev
		}

custom_cost_trigger = {
            command_power > 24
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -25
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_gubarev
            }
        }
		custom_cost_text = decision_cost_CP_25
remove_effect = {
random_list = {
 10 = {
 country_event = { id = dan.35 days = 2 } }
 90 = {
 country_event = { id = dan.36 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
		
	kill_gubarev_2 = {
	
			ai_will_do = {	
			factor = 30		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_gubarev
		}

custom_cost_trigger = {
            command_power > 49
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -50
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_gubarev
            }
        }
		custom_cost_text = decision_cost_CP_50
remove_effect = {
random_list = {
 30 = {
 country_event = { id = dan.35 days = 2 } }
 70 = {
 country_event = { id = dan.36 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
	kill_gubarev_3 = {
	
			ai_will_do = {	
			factor = 80		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_gubarev
		}

custom_cost_trigger = {
            command_power > 74
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -75
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_gubarev
            }
        }
		custom_cost_text = decision_cost_CP_75
remove_effect = {
random_list = {
 50 = {
 country_event = { id = dan.35 days = 2 } }
 50 = {
 country_event = { id = dan.36 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
		
kill_girkin = {			

		icon = GFX_decision_eng_trade_unions_support
		
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}		

		visible = {	
		tag = AFU
		}		

		cost = 50
		fire_only_once = yes

		remove_effect = {
		set_country_flag = try_to_kill_girkin
	   }
		ai_will_do = {	
			factor = 100		
		}
   }
   
	kill_girkin_1 = {
	
			ai_will_do = {	
			factor = 10		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}
		visible = {
        has_country_flag = try_to_kill_girkin
		}

custom_cost_trigger = {
            command_power > 24
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -25
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_girkin
            }
        }
		custom_cost_text = decision_cost_CP_25
remove_effect = {
random_list = {
 10 = {
 country_event = { id = dan.37 days = 2 } }
 90 = {
 country_event = { id = dan.38 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
		
	kill_girkin_2 = {
	
			ai_will_do = {	
			factor = 20		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_girkin
		}

custom_cost_trigger = {
            command_power > 49
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -50
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_girkin
            }
        }
		custom_cost_text = decision_cost_CP_50
remove_effect = {
random_list = {
 30 = {
 country_event = { id = dan.37 days = 2 } }
 70 = {
 country_event = { id = dan.38 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
	kill_girkin_3 = {
	
			ai_will_do = {	
			factor = 90		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_girkin
		}

custom_cost_trigger = {
            command_power > 74
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -75
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_girkin
            }
        }
		custom_cost_text = decision_cost_CP_75
remove_effect = {
random_list = {
 50 = {
 country_event = { id = dan.37 days = 2 } }
 50 = {
 country_event = { id = dan.38 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
		
kill_tsaryov = {			

		icon = GFX_decision_eng_trade_unions_support
		
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}		

		visible = {	
		tag = AFU
		}		

		cost = 50
		fire_only_once = yes

		remove_effect = {
		set_country_flag = try_to_kill_tsaryov
	   }
		ai_will_do = {	
			factor = 100		
		}
   }
   
	kill_tsaryov_1 = {
	
			ai_will_do = {	
			factor = 10		
		
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = { tag = AFU	
		NOT = { has_country_flag = operation_start } 
		}
		visible = {
        has_country_flag = try_to_kill_tsaryov
		}

custom_cost_trigger = {
            command_power > 24
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -25
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_tsaryov
            }
        }
		custom_cost_text = decision_cost_CP_25
remove_effect = {
random_list = {
 10 = {
 country_event = { id = dan.39 days = 2 } }
 90 = {
 country_event = { id = dan.40 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
	}
		
	kill_tsaryov_2 = {
	
			ai_will_do = {	
			factor = 20		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_tsaryov
		}

custom_cost_trigger = {
            command_power > 49
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -50
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_tsaryov
            }
        }
		custom_cost_text = decision_cost_CP_50
remove_effect = {
random_list = {
 30 = {
 country_event = { id = dan.39 days = 2 } }
 70 = {
 country_event = { id = dan.40 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}
	kill_tsaryov_3 = {
	
			ai_will_do = {	
			factor = 90		
		}
	
	icon = GFX_decision_generic_ignite_civil_war
	
		available = {
 tag = AFU
 		NOT = {
		has_country_flag = operation_start
		}
}
		visible = {
        has_country_flag = try_to_kill_tsaryov
		}

custom_cost_trigger = {
            command_power > 74
        }
		fire_only_once = yes
		days_remove = 100
        complete_effect = {
            hidden_effect = {
                add_command_power = -75
				set_country_flag = operation_start
				clr_country_flag = try_to_kill_tsaryov
            }
        }
		custom_cost_text = decision_cost_CP_75
remove_effect = {
random_list = {
 50 = {
 country_event = { id = dan.39 days = 2 } }
 50 = {
 country_event = { id = dan.40 days = 2 } }
}
           hidden_effect = {
				clr_country_flag = operation_start
            }

}
		}

}