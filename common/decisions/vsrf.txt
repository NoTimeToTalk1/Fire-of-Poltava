vsrf_decisions = {
    vsr_pabeda = {			

		icon = GFX_ZAK_rada_election
		
		
		available = { tag = VSR	
		NOT = {
		country_exists = AFU
		}
		
		}
		
		fire_only_once = yes	

		visible = {	
		NOT = {
		country_exists = AFU
		country_exists = DPR
		country_exists = LPR
		}
		tag = VSR
		}		


		remove_effect = {
		add_political_power = 20
			hidden_effect = { country_event = vsrf.1
			}
	   }
		ai_will_do = {	
			factor = 100		
		}
   }
    vsr_annex_dpr_and_lpr = {			

		icon = GFX_ZAK_rada_election
		
		
		available = { tag = VSR	
	OR = {
		country_exists = DPR
		country_exists = LPR
	}
		
		}
		
		fire_only_once = yes	

		visible = {	
		OR = {
		country_exists = DPR
		country_exists = LPR
		}
		tag = VSR
		}		


		remove_effect = {
		add_political_power = 20
annex_country = {
    target = DPR
    transfer_troops = yes
}
annex_country = {
    target = LPR
    transfer_troops = yes
}
			hidden_effect = {
		every_state = {
			limit = {
				is_core_of = DPR
			}
			remove_core_of = DPR
			add_core_of = VSR
		}
		every_state = {
			limit = {
				is_core_of = LPR
			}
			remove_core_of = LPR
			add_core_of = VSR
		}			
			LPR = {
every_character = {
                set_nationality = {
                    target_country = VSR
				
                }
            }
}
DPR = {
every_character = {
                set_nationality = {
                    target_country = VSR
				
                }
            }
}
			}
	   }
		ai_will_do = {	
			factor = 0		
		}
   }
    vsr_mobilization_1 = {			

		icon = GFX_ZAK_rada_election
		
		
		available = { tag = VSR	}
		
		fire_only_once = yes
        days_remove = 15		

		visible = {	
		tag = VSR
		}		

		cost = 50

		remove_effect = {
		    add_manpower = 2000
			hidden_effect = { add_manpower = 1000
			}
		set_country_flag = {
		flag = VSR_1
		days = 365
		value = 1
		}
	   }
		ai_will_do = {	
			factor = 100		
		}
   }
vsr_mobilization_2 = {				

		icon = GFX_ZAK_rada_election
		
		
		available = { tag = VSR }
		
		fire_only_once = yes
        days_remove = 20		
		days_re_enable = 25	

		visible = {	
		tag = VSR
		has_country_flag = VSR_1	
		}		

		cost = 60


		remove_effect = {
		    add_manpower = 5000
			hidden_effect = { add_manpower = 5 }
		set_country_flag = {
		flag = VSR_2
		days = 365
		value = 1
		}
		}
		ai_will_do = {	
			factor = 100		
		}
   }
vsr_mobilization_3 = {			

		icon = GFX_ZAK_rada_election
		
		
		available = { tag = VSR }
		
		fire_only_once = yes
        days_remove = 30		
		days_re_enable = 25	

		visible = {	
		tag = VSR
		has_country_flag = VSR_2	
		}		

		cost = 60


		remove_effect = {
		    add_manpower = 15000
		set_country_flag = {
		flag = VSR_3
		days = 365
		value = 1
		}
		add_ideas = extensive_conscription
	}
		ai_will_do = {	
			factor = 100		
		}
   }
}